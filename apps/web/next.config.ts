/** @type {import('next').NextConfig} */
const nextConfig = {
  // ✅ 頂層，非 experimental
  allowedDevOrigins: [
    '172.28.191.117',      
    'localhost',          
  ],
};

export default nextConfig
